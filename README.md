package olá.mundo;// olámundo-> n deveria ter . só olámundo é um pacote

/*primeira letra miascula? só pode ser Classe oi Interface */

public class OláMundo {//classe OláMundo 2 letra é uma classe
    public static void main (String [] args){//mais -> metado 
        System.out.print("Olá,mundo! and Hello word!");
    }
}
